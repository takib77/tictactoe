'use strict';

(function xx() {
    const sectorSelector = document.querySelectorAll('td.sector');
        sectorSelector.addEventListener('click', () => {
            sectorSelector.textContent = 'X';
        })
        console.log(sectorSelector);
})();






